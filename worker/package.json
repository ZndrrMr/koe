{
  "name": "koe-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Edge proxy for Koe (Inworld TTS, Soniox STT, Claude, Gemini).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "tsx --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.12.14"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.12",
    "typescript": "^5.9.0",
    "wrangler": "^4.123.0"
  }
}
